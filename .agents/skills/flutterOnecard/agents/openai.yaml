interface:
  display_name: "FlutterOnecard"
  short_description: "Repo-specific patterns and workflows for flutterOnecard"
  default_prompt: "Use the flutterOnecard repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true