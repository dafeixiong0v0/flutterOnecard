---
name: update-ios-build-workflow
description: Workflow command scaffold for update-ios-build-workflow in flutterOnecard.
allowed_tools: ["Bash", "Read", "Write", "Grep", "Glob"]
---

# /update-ios-build-workflow

Use this workflow when working on **update-ios-build-workflow** in `flutterOnecard`.

## Goal

Updates the iOS build workflow configuration for CI/CD.

## Common Files

- `.github/workflows/ios_build.yml`

## Suggested Sequence

1. Understand the current state and failure mode before editing.
2. Make the smallest coherent change that satisfies the workflow goal.
3. Run the most relevant verification for touched files.
4. Summarize what changed and what still needs review.

## Typical Commit Signals

- Edit the .github/workflows/ios_build.yml file to adjust build steps or environment.
- Commit the changes with a message referencing ios_build.yml.

## Notes

- Treat this as a scaffold, not a hard-coded script.
- Update the command if the workflow evolves materially.